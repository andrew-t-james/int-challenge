import React from 'react';

import './Robots.css';

const Robots = () => {
  return (
    <section className="robot-container">
      <p>Add some Robots</p>
    </section>
  );
};

export default Robots;
